// Given a string, return a new string with the reversed order of characters

function reverse(str) {
	return str.split('').reverse().join('');
}

// This function takes the provided string, convert the string to an array
// of characters, reverse the array, then turn the reversed array back into a string